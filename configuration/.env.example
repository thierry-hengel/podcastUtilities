# ==============================================================================
# ENVIRONMENT FILE (.env)
# Contains all secrets and tokens required by the Python modules
# ==============================================================================

# --- MQTT (my_mqtt.py) --------------------------------------------------------
# The username and password for the MQTT broker connection.
MQTT_USER=""
MQTT_PASSWORD=""

# --- MARIADB (my_mariadb.py) --------------------------------------------------
# The username and password for the database connection.
MARIADB_USER=""
MARIADB_PASSWORD=""

# --- FACEBOOK (my_facebook.py) ------------------------------------------------
# The Facebook Page Access Token required to post messages.
FACEBOOK_TOKEN=""

# --- X (formerly Twitter) (my_x.py) -------------------------------------------
# API Keys and Access Tokens required for the X API.
X_CONSUMER_KEY=""
X_CONSUMER_SECRET=""
X_ACCESS_TOKEN=""
X_ACCESS_TOKEN_SECRET=""

# --- DISCORD (my_discord.py) --------------------------------------------------
# The full Discord Webhook URL for sending messages.
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/xyz"

# --- BITLY (my_bitly.py) ------------------------------------------------------
# The Bitly OAuth Token for generating short URLs.
BITLY_TOKEN=""

# --- HOME ASSISTANT (my_home_assistant.py) ------------------------------------
# Long-Lived Access Token for the Home Assistant API.
HOMEASSISTANT_TOKEN=""

# --- YOUTUBE (my_youtube.py) --------------------------------------------------
# Credentials required for the YouTube Data API (v3) and Analytics API (v2).
YOUTUBE_CLIENT_ID=""
YOUTUBE_CLIENT_SECRET=""
YOUTUBE_REFRESH_TOKEN=""